<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
<mapper namespace="kr.co.dw.Mapper.MemberMapper">
  <select id="insert">
    INSERT INTO MEMBER (id, name) VALUES (SEQ_MEMBER_ID.NEXTVAL,#{name})
  </select>
</mapper>